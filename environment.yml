name: rethinking-cross-channel
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - pip:
      - numpy>=1.22,<2.0
      - pandas>=1.4,<3.0
      - scipy>=1.8,<2.0
      - matplotlib>=3.5,<4.0
      - torch==2.4.1
      - jupyter>=1.0
      - ipykernel>=6.0
